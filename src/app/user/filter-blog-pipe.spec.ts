import { FilterBlogPipe } from './filter-blog-pipe';

describe('FilterBlogPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBlogPipe();
    expect(pipe).toBeTruthy();
  });
});
