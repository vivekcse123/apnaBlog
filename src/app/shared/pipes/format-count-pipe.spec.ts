import { FormatCountPipe } from './format-count-pipe';

describe('FormatCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCountPipe();
    expect(pipe).toBeTruthy();
  });
});
